services:
  ntg:
    build: .
    ports:
      - 8888:8888
    volumes:
      - .:/project
      - ../heightmaps/uncorrupted_split_heightmaps_second_pass:/heightmaps
    deploy:
      resources:
        reservations:
          devices:
          - capabilities: [gpu]