services:
  ntg:
    build: .
    image: ntg
    container_name: ntg
    tty: true
    volumes:
      - .:/project
      - ../heightmaps:/heightmaps
    deploy:
      resources:
        reservations:
          devices:
          - capabilities: [gpu]